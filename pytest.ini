[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --ignore=tests/legacy --strict-markers --strict-config
markers =
    unit: Unit tests
    integration: Integration tests requiring real API access